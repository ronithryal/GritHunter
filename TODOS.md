# GritHunter v1 — Engineering TODOs

Generated by /plan-eng-review on 2026-04-21. Ordered by dependency.

  1. Unit tests (classifyInput, parseEnrichment, etc.) — write these alongside the code as you build each function.                                                                                                                              
  2. Integration tests (/api/search, /api/enrich/:handle) — write these as you build the routes.                                                                                                                                                 
  3. E2E / Manual checklist — run this against the real APIs before launch.                                                                                                                                                                         
  The pre-condition gate at the top of the manual checklist is the exception — that one runs before any code is written.  

## Before writing any product code

- [ ] **Run pre-condition gate.** Write a 20-line script hitting the Perplexity Agent API.
      Query: "Find React Native engineers in San Francisco who have shipped apps to the App Store."
      Pass criteria: ≥3 of 5 returned developers have at least one independently verifiable URL.
      If this fails, stop and rethink the architecture before continuing.

- [ ] **Provision credentials:** PERPLEXITY_API_KEY, GITHUB_TOKEN, UPSTASH_REDIS_REST_URL, UPSTASH_REDIS_REST_TOKEN

- [ ] **Validate Perplexity Agent API cost per query.** ~6 calls per user query. Calculate $/query
      against the target: must support ≤$50/month for light users (20 queries/day).

## Project scaffold

- [ ] `npx create-next-app@latest grithunter --typescript --tailwind --app`
- [ ] Add dependencies: `@upstash/redis`, `zod` (for EvidenceCard schema validation)
- [ ] Set up `.env.local` with all 4 credentials
- [ ] Deploy empty Next.js app to Vercel, confirm push-to-deploy works

## Core library

- [ ] `src/lib/classifyInput.ts` — regex classifier returning 'profile' | 'repo' | 'nl'
- [ ] `src/lib/parseEnrichment.ts` — strip markdown fences, JSON.parse, url validation, 6-signal cap
- [ ] `src/lib/topLanguages.ts` — compute top 3 languages by repo count from repos array
- [ ] `src/lib/rateLimitCheck.ts` — Redis-backed IP rate limit + daily spend cap check

## API routes

- [ ] `src/app/api/search/route.ts`
      - Input classification
      - Rate limit check (before Perplexity)
      - Perplexity Agent API call (system+user envelope, 1 retry)
      - Parallel GitHub handle validation (Promise.all, drop 404s)
      - Return { handles: string[], total: number }

- [ ] `src/app/api/enrich/[handle]/route.ts`
      - MGET Redis cache check (batch all 5 if called server-side, or check single)
      - Cache hit: return stored EvidenceCard
      - Cache miss: Perplexity Agent API enrichment call (system+user envelope, 1 retry)
      - parseEnrichment() → validate JSON, strip fences, validate URLs
      - Parallel GitHub metadata fetch (GET /users/:handle + GET /users/:handle/repos)
      - Assemble EvidenceCard, write to cache (TTL: 86400s), return

## Frontend

- [ ] Mode toggle component (Search / Similar to...)
- [ ] Search input with placeholder text per mode
- [ ] POST /api/search on submit
- [ ] Fire 5 parallel /api/enrich/:handle calls on handle list receipt
- [ ] Render DevCard component as each fetch resolves (progressive loading)
- [ ] DevCard: avatar initials, handle, location, WHY paragraph, signal tags, last_verified_at
- [ ] Signal tag colors: repo=blue, blog=green, npm=orange, talk=purple, so=yellow, hn=red, x=gray
- [ ] Error states: 429, 503, no results, evidence unavailable, invalid URL, mode hint

## Tests

- [ ] `src/lib/classifyInput.test.ts` (see test-plan.md)
- [ ] `src/lib/parseEnrichment.test.ts`
- [ ] `src/lib/topLanguages.test.ts`
- [ ] `src/lib/rateLimitCheck.test.ts`
- [ ] `test/search.integration.test.ts` (msw mocks)
- [ ] `test/enrich.integration.test.ts` (msw mocks)

## Pre-launch checklist

- [ ] Manual E2E against real APIs (see test-plan.md)
- [ ] Pre-condition gate passing (≥3/5 verifiable)
- [ ] First card appears within 10s on real query
- [ ] All 5 cards within 30s
- [ ] Rate limiting works (11th query blocked)
- [ ] Prompt injection test: query containing "Ignore previous instructions" returns normal results
- [ ] Wireframe committed to repo: `cp /tmp/grithunter-sketch.html public/sketch.html`
